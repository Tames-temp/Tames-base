using System.Collections;
using System.Collections.Generic;
using UnityEngine;
namespace Markers
{
    public class MarkerFaceCamera : MonoBehaviour
    {
        // Start is called before the first frame update
        public bool restrictY = true;
    }
}